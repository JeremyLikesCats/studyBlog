# Introduction, Vector Functions and Electrostatics

## Vector Functions
Vector functions are simply functions that can be expressed like this:
$$
\mathbb{F}(x, y, z) = F_x(x, y, z) \hat{i} + F_y(x, y, z) \hat{j} + F_z(x, y, z) \hat{k}.
$$
